# Codebase Cleanup — 2026-06-06

Files and directories removed during dead-code audit. Mobile app was excluded from scope.

---

## Tracked files (git rm)

| File | Reason |
|---|---|
| `apps/django-api/SCRAPER_FINDINGS.md` | Development notes, not part of the app |
| `apps/django-api/inspect_quickmart_fresh.py` | One-off DOM inspection script used during scraper development |
| `apps/django-api/quickmart_scraper.py` | Prototype — superseded by `scrapers/quickmart.py` |
| `apps/django-api/quickmart_v2.py` | Prototype — superseded by `scrapers/quickmart.py` |

---

## Untracked files (deleted, never committed)

### One-off debug / inspection scripts — `apps/django-api/`

| File | Reason |
|---|---|
| `check_db.py` | Ad-hoc DB table count inspector |
| `check_deals.py` | Deal count / recent-scrape inspector |
| `check_progress.py` | Live Quickmart scrape progress monitor |
| `cleanup_dupes.py` | One-time fix for duplicate `StagingProduct` rows |
| `cleanup_runs.py` | One-time script to mark stale runs as failed |
| `count_branches.py` | Quickmart branch URL count inspector |
| `inspect_naivas.py` | Naivas Livewire HTML structure inspector |
| `inspect_quickmart.py` | Quickmart product card DOM selector probe |
| `rediscover_branches.py` | One-off branch re-discovery with fixed regex |
| `scrape_quickmart_parallel.py` | Parallel threaded scraper — development experiment |
| `test_naivas.py` | Manual Naivas scraper test script |
| `test_quickmart.py` | Manual Quickmart scraper test script |

### Large data files — `apps/django-api/`

| File / Dir | Reason |
|---|---|
| `local_data.json` (~146 MB) | Django fixture dump — not used by the app |
| `quickmart_output/` | CSV artifacts from a past development scrape run |

### Root-level duplicate directories

| Directory | Reason |
|---|---|
| `csv/` | Historical CSV exports (`naivas.csv`, `quickmart.csv`) — no production import |
| `scraper/` | Old reference copy of `apps/django-api/scrapers/` — exact duplicate |
| `shopping_list/` | Old reference copy of `apps/django-api/shopping_list/` — exact duplicate |
| `shopping_list_ui/` | Stale UI component reference — never imported by web or mobile app |

### Database files

| File | Reason |
|---|---|
| `db.sqlite3` (root) | Local dev SQLite — production uses PostgreSQL |
| `apps/django-api/db.sqlite3` | Older local dev SQLite copy |

### Build artifacts

| File | Reason |
|---|---|
| `apps/web/tsconfig.tsbuildinfo` | TypeScript incremental build cache — auto-regenerated |

### One-off SSH / deployment scripts — `scripts/`

| File | Reason |
|---|---|
| `check_logs.py` | SSH script to read GitHub Actions logs on the runner |
| `check_pods.py` | SSH script to tail k8s pod logs |
| `debug_web.py` | SSH script to inspect Docker build stages for the web app |
| `register_runner.py` | One-time GitHub runner registration on the VPS |
| `test_build.py` | SSH script to build images and apply k8s manifests manually |
| `watch_deploy.py` | SSH script to monitor CI/CD deployment progress |

---

## What was kept

| File | Reason kept |
|---|---|
| `apps/django-api/manage.py` | Django entrypoint |
| `apps/django-api/map_products_direct.py` | Admin utility — re-categorises products that lack `master_category` |
| `scripts/deploy_remote.py` | Manual full-stack deploy via SSH (emergency fallback) |
| `scripts/bootstrap-server.sh` | Server initial setup script |
| `scripts/install-github-runner.sh` | GitHub Actions runner installation |
